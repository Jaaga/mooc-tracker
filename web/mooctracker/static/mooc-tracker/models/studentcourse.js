// global backbone app
var app = app || {};

(function(){
  'use strict';

  // the student course model
  // dont confuse with 'course' model
  // that one is added by the admin
  // this is the one that'll be populated in students -
  // select box

  app.StudentCourse = Backbone.Model.extend({
    // strange! strange! no defaults
  });


})();